<% provide(:title, "Home") %>

<div class="row" id="home-page">
	

	<div class="col-md-6 col-md-offset-3">
		<ol class="statuses">
			<%= render 'shared/status_form' %>

			
			<%= render @statuses %>	
		
		<%= will_paginate @statuses if @statuses.any? %>	
		</ol>

		
	</div>

</div>